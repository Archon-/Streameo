@if(Request.IsAuthenticated) {
    <text>Zalogowany jako: <strong>@User.Identity.Name</strong>!
    [ @Html.ActionLink("Wyloguj", "LogOff", "Account") ] 
    @if (User.IsInRole("Admin"))
    { 
        <text>
        | [ @Ajax.ActionLink("Panel admina", "Index", "Admin", new AjaxOptions()
                       {
                           InsertionMode = InsertionMode.Replace,
                           UpdateTargetId = "main",
                           HttpMethod = "GET"
                       }) ]
        </text>
    }
     <form action="@Streameo.Models.Dotpay.LinkToPay" method="post">

<input type="hidden" name="kwota" value="@Streameo.Models.Premium.Amount" />
<input type="hidden" name="opis" value="@Streameo.Models.Premium.Description" />
<input type="hidden" name="control" value="@Streameo.Models.Premium.Control(User.Identity.Name)" />
<button type="submit">Buy premium account!</button>
</form>
    
    
    </text>
   
}
else
{
    @:[ @Ajax.ActionLink("Rejestracja", "Register", "Account", new AjaxOptions()
                       {
                           InsertionMode = InsertionMode.Replace,
                           UpdateTargetId = "main",
                           HttpMethod = "GET"
                       }) ]
    @:[ @Ajax.ActionLink("Zaloguj", "LogOn", "Account", new AjaxOptions()
                       {
                           InsertionMode = InsertionMode.Replace,
                           UpdateTargetId = "main",
                           HttpMethod = "GET"
                       }) ]
    
    @:[ @Html.ActionLink("Zaloguj przez Facebooka", "Facebook", "Account") ]
}
